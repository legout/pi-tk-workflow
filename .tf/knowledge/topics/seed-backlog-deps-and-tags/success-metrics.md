# Success Metrics: backlog-deps-and-tags

- `/tf-backlog` creates tickets with `deps` populated automatically
- Component tags are present on >80% of generated tickets
- Related tickets are linked via `tk link` where appropriate
- `tk ready` and `tk blocked` show meaningful results immediately after backlog creation
- Ralph loop can use component tags for parallel processing without manual tagging
- Users report reduced manual setup time for new backlogs
